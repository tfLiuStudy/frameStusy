<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.al.mybatisPrac.dao.UserDao">
    <!--查询用户-->
    <select id="findUserById" resultType="cn.al.mybatisPrac.domain.User" parameterType="int">
        select * from user where id=#{id}
    </select>
</mapper>